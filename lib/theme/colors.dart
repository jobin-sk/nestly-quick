import 'package:flutter/material.dart';

class AppColors {
  // primary
  static const Color primary = Color(0xFF7C3AED);
  static const Color primaryLight = Color(0xFFEDE9FE);
  static const Color primaryLighter = Color(0xFFFDF4FF);

  // dark text
  static const Color dark = Color(0xFF1E1B4B);
  static const Color subtext = Color(0xFF7C6FAA);

  // pink accent (shared lists, highlights)
  static const Color pink = Color(0xFFE879F9);
  static const Color pinkLight = Color(0xFFFCE7F3);

  // background
  static const Color background = Color(0xFFFFFFFF);
  static const Color backgroundAlt = Color(0xFFFDF4FF);

  // Borders colors
  static const Color border = Color(0xFFEDE9FE);

  // Category tints
  // probably should add more colors
  static const Color catBlue = Color(0xFFDBEAFE);
  static const Color catYellow = Color(0xFFFEF9C3);
  static const Color catPink = Color(0xFFFCE7F3);
  static const Color catGreen = Color(0xFFDCFCE7);

  // Destructive
  static const Color danger = Color(0xFFDC2626);
  static const Color dangerLight = Color(0xFFFEE2E2);
}
